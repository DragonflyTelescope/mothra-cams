services:
  observatory-camera:
    build: .
    container_name: observatory-camera
    volumes:
      - - /home/mothra/git/mothra-cams/html:/data/observatory
      - /dev/bus/usb:/dev/bus/usb
    devices:
      - /dev/bus/usb
    privileged: true
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - TZ=America/Santiago
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"